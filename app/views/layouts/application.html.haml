!!!
%html
  %head
    %meta{ charset: 'utf-8'}
    %meta{ content: 'IE=Edge,chrome=1', http: { equiv: 'X-UA-Compatible' } }
    %meta{ name: 'viewport', content: 'width=device-width, initial-scale=1.0' }
    -# General
    %meta{ name: 'description', content: 'Database Access for the Division of Sleep and Circadian Rhythms at BWH' }
    %meta{ name: 'author', content: '' }
    %meta{ name: 'keywords', content: @keywords.present? ? @keywords : 'sleep research, sleep medicine, database' }

    %title
      = [@title, 'BWH Sleep Access'].compact.join(' - ').html_safe

    = favicon_link_tag
    = csrf_meta_tags

    / HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries
    /[if lt IE 9]
  %script{ src: '//oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js' }
    %script{ src: '//oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js' }

    = stylesheet_link_tag 'application', media: 'all' #, data: { turbolinks_track: true }
    = javascript_include_tag 'application' #, data: { turbolinks_track: true }
    = javascript_tag "var root_url='#{request.script_name + '/'}';"
  %body{ class: yield(:body_class) }
    = yield :header

    #top-menu= render 'layouts/menu'

    = yield :banner

    .container.body-content
      = render 'layouts/flash_messages'

      = yield

    = yield :call_to_action

    .footer-container
      .container
        = render 'layouts/footer'