require 'spec_helper'

describe <ModelName> do

  context "Associations" do
    before :each do
      create(<:model_name>)
    end
  end

  context "Validations" do
    before :each do
      create(<:model_name>)
    end
  end

end
